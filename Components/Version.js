import React from 'react';

export default function Version({children}) {
    return ( <div>
            {children}
            <div class="version">(Em Desenvolvimento) Versão 0.3</div>
        </div>
    )
}